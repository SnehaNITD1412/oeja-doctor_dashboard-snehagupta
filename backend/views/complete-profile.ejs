<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete Your Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .profile-container {
            max-width: 800px;
            margin: 50px auto;
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }
        .section-title {
            color: #0d6efd;
            border-bottom: 2px solid #0d6efd;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>

<div class="profile-container">
    <h1 class="text-center mb-4">Complete Your Doctor Profile</h1>
    <p class="text-center text-muted mb-4">Please fill in your professional details to proceed to your dashboard.</p>
    
    <form id="doctor-profile-form" action="/complete-profile" method="POST" enctype="multipart/form-data">

        <fieldset class="mb-4">
            <h3 class="section-title">Professional Information</h3>
            <div class="mb-3">
                <label for="regNumber" class="form-label">Medical Registration Number*</label>
                <input type="text" class="form-control" id="regNumber" name="registrationNumber" placeholder="Enter your registration number" required>
            </div>
             <div class="mb-3">
                <label for="specialization" class="form-label">Specialization*</label>
                <select class="form-select" id="specialization" name="specialization" required>
                    <option value="" selected disabled>Select Specialization</option>
                    <option value="Cardiologist">Cardiologist</option>
                    <option value="Dermatologist">Dermatologist</option>
                    <option value="General Physician">General Physician</option>
                    <option value="Neurologist">Neurologist</option>
                    <option value="Pediatrician">Pediatrician</option>
                </select>
            </div>
             <div class="mb-3">
                <label for="contactNumber" class="form-label">Contact Number*</label>
                <input type="tel" class="form-control" id="contactNumber" name="phoneNumber" placeholder="Enter 10-digit mobile number" required pattern="[0-9]{10}">
            </div>
            <div class="mb-3">
                <label for="clinicAddress" class="form-label">Hospital/Clinic Address*</label>
                <textarea class="form-control" id="clinicAddress" name="clinicAddress" rows="3" placeholder="Enter full address" required></textarea>
            </div>
            <div class="mb-3">
                <label for="verificationDocument" class="form-label">Upload Verification Document*</label>
                <input class="form-control" type="file" id="verificationDocument" name="verificationDocument" accept=".pdf,.png,.jpg,.jpeg" required>
                <div class="form-text">Please upload your medical registration certificate (PDF, PNG, JPG). Max size: 2MB.</div>
            </div>
        </fieldset>

        <div class="d-grid">
            <button type="submit" class="btn btn-primary btn-lg">Submit Profile</button>
        </div>

    </form>
</div>

</body>
</html>
